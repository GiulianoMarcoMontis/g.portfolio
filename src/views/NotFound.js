const NotFound = () => {
  return (
    <section className="notFound section bd-container bd-grid">
      <div>
        <span className="section-subtitle">Sorry</span>
        <h2 className="section-title">This URL does not exist!!</h2>
      </div>
    </section>
  );
};

export default NotFound;
